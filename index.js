console.log('funguju!');
const side = 1

const changeSide = document.querySelector(img)


changeSide.textContent(`<img class="dice" src="img/side${
    changeSide.addEventListener("keydown", () => {
        return side + 1
    })
}.svg" /`>)